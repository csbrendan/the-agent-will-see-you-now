# MedBridge — Python dependencies
# Built/tested on Python 3.9.16 (use Python 3.9+). Pinned to the working environment.

# Core: Claude API + config
anthropic==0.117.0
python-dotenv==1.2.1

# Structured outputs / workflows
pydantic==2.13.4
PyYAML==6.0.3

# Vision / frames (Visual Evidence Extractor)
opencv-python-headless==5.0.0.93
Pillow==11.3.0
numpy==2.0.2

# Audio (Listener) — speech items (LOC-Questions, Language, Dysarthria)
faster-whisper==1.2.1

# OCR for NINDS title-card auto-segmentation — macOS only (Apple Vision).
# Only needed to RE-run ninds_segmenter.py; skip on non-Mac.
ocrmac==1.0.1

# Datasets / Hugging Face (NINDS + ProGait pulls)
datasets==4.5.0
huggingface_hub==1.8.0

# Data / analysis
pandas==2.3.3

# UI — kept thin (must NOT be "the feature")
streamlit

# Optional: bundled ffmpeg binary if the system has no `ffmpeg` (clip cutting).
# imageio-ffmpeg
